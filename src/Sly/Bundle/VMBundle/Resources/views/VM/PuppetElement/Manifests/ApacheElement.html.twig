{% if vm.php %}
class {"apache": }
{% else %}
class { "apache::mod::php": }
{% endif %}

apache::vhost { "{{ vm.hostname }}":
    priority      => "10",
    vhost_name    => "{{ vm.hostname }}",
    port          => "{{ vm.apachePort }}",
    docroot       => "{{ vm.apacheRootDir }}",
    serveradmin   => "chuck.norris@{{ vm.hostname }}",
    serveraliases => [ "" ],
}
